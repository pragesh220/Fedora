# 1. Clone the repo, but don't pull files yet
git clone --depth 1 --filter=blob:none --sparse https://github.com/ryanoasis/nerd-fonts.git

# 2. Enter the repo
cd nerd-fonts

# 3. Tell git you only want the patched-fonts folder
git sparse-checkout set patched-fonts
